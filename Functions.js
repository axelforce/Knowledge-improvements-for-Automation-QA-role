const firstName = 'Roy';
const lastName = 'Jons';

function printInConsole(firstName, lastName) {
  console.log(`What a nice name is ${firstName}! And how well it goes with your surname ${lastName}`);
}

printInConsole(firstName, lastName);
